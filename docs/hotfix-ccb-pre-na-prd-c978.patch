diff --git a/iMPS_platform/src/app/dashboard/pm-report/ccb/input_PMreport/components/checkList.tsx b/iMPS_platform/src/app/dashboard/pm-report/ccb/input_PMreport/components/checkList.tsx
--- a/iMPS_platform/src/app/dashboard/pm-report/ccb/input_PMreport/components/checkList.tsx
+++ b/iMPS_platform/src/app/dashboard/pm-report/ccb/input_PMreport/components/checkList.tsx
@@ -1472,7 +1472,7 @@ export default function CCBPMReport() {
     const missingInputs = useMemo(() => {
         const r: string[] = [];
         // Main breaker (Q9)
-        if (rows["r9_main"]?.pf !== "NA") {
+        if (rows["r9_main"]?.pf !== "NA" && (!isPostMode || rowsPre["r9_main"]?.pf !== "NA")) {
             VOLTAGE_FIELDS_CCB.forEach((k) => {
                 const v = mMain.state[k]?.value ?? "";
                 if (!String(v).trim()) r.push(`9 – ${LABELS[k]}`);
@@ -1481,7 +1481,7 @@ export default function CCBPMReport() {
         // Sub breakers (Q10)
         for (let i = 0; i < subBreakerCount; i++) {
             const rowKey = `r10_sub${i + 1}`;
-            if (rows[rowKey]?.pf === "NA") continue;
+            if (rows[rowKey]?.pf === "NA" || (isPostMode && rowsPre[rowKey]?.pf === "NA")) continue;
             const m = M_SUB_LIST[i];
             VOLTAGE_FIELDS_CCB.forEach((k) => {
                 const v = m.state[k]?.value ?? "";
@@ -1490,13 +1490,13 @@ export default function CCBPMReport() {
             });
         }
         return r;
-    }, [mMain.state, mSub1.state, mSub2.state, mSub3.state, mSub4.state, mSub5.state, mSub6.state, rows, subBreakerCount, lang]);
+    }, [mMain.state, mSub1.state, mSub2.state, mSub3.state, mSub4.state, mSub5.state, mSub6.state, rows, rowsPre, subBreakerCount, lang, isPostMode]);
 
     // Detailed missing inputs for PMValidationCard
     const missingInputsDetailed = useMemo(() => {
         const r: { qNo: number; subNo?: number; label: string; fieldKey: string }[] = [];
         // Main breaker (Q9)
-        if (rows["r9_main"]?.pf !== "NA") {
+        if (rows["r9_main"]?.pf !== "NA" && (!isPostMode || rowsPre["r9_main"]?.pf !== "NA")) {
             VOLTAGE_FIELDS_CCB.forEach((k) => {
                 const v = mMain.state[k]?.value ?? "";
                 if (!String(v).trim()) r.push({ qNo: 9, label: LABELS[k], fieldKey: k });
@@ -1505,7 +1505,7 @@ export default function CCBPMReport() {
         // Sub breakers (Q10)
         for (let i = 0; i < subBreakerCount; i++) {
             const rowKey = `r10_sub${i + 1}`;
-            if (rows[rowKey]?.pf === "NA") continue;
+            if (rows[rowKey]?.pf === "NA" || (isPostMode && rowsPre[rowKey]?.pf === "NA")) continue;
             const m = M_SUB_LIST[i];
             VOLTAGE_FIELDS_CCB.forEach((k) => {
                 const v = m.state[k]?.value ?? "";
@@ -1514,7 +1514,7 @@ export default function CCBPMReport() {
             });
         }
         return r;
-    }, [mMain.state, mSub1.state, mSub2.state, mSub3.state, mSub4.state, mSub5.state, mSub6.state, rows, subBreakerCount]);
+    }, [mMain.state, mSub1.state, mSub2.state, mSub3.state, mSub4.state, mSub5.state, mSub6.state, rows, rowsPre, subBreakerCount, isPostMode]);
 
     const allRequiredInputsFilled = missingInputs.length === 0;
     const isSummaryFilled = summary.trim().length > 0;
